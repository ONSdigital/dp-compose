services:
  elasticsearch:
    extends:
      file: ${PATH_MANIFESTS}/deps/elasticsearch.yml
      service: elasticsearch
  redis:
    extends:
      file: ${PATH_MANIFESTS}/deps/redis.yml
      service: redis
