services:
  mongodb:
    extends:
      file: ${PATH_MANIFESTS}/deps/mongo.yml
      service: mongodb
    volumes:
      - ${PATH_PROVISIONING}/mongo:/docker-entrypoint-initdb.d:ro
      - legacy_core_publishing_data:/data/db
volumes:
  legacy_core_publishing_data: {}
