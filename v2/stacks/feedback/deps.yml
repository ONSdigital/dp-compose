services:
  elasticsearch:
    extends:
      file: ${PATH_MANIFESTS}/deps/elasticsearch.yml
      service: elasticsearch
  mailhog:
    extends:
      file: ${PATH_MANIFESTS}/deps/mailhog.yml
      service: mailhog
